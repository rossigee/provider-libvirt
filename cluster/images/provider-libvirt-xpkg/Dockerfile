# Crossplane Provider xpkg image
# This image includes the package.yaml required by Crossplane for package installation
FROM ghcr.io/rossigee/provider-libvirt:latest

# Crossplane Provider package metadata
COPY package.yaml /package.yaml