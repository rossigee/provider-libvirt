---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: domains.libvirt.crossplane.io
spec:
  group: libvirt.crossplane.io
  names:
    kind: Domain
    listKind: DomainList
    plural: domains
    singular: domain
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.atProvider.state
      name: STATE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Domain is a libvirt virtual machine domain.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DomainSpec defines the desired state of Domain
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: DomainParameters are the configurable fields of a Domain.
                properties:
                  arch:
                    default: x86_64
                    description: Architecture
                    type: string
                  autostart:
                    default: false
                    description: Autostart domain on host boot
                    type: boolean
                  boot:
                    description: Boot devices in order of preference
                    items:
                      type: string
                    type: array
                  console:
                    description: Console configuration
                    properties:
                      sourcePath:
                        description: Source path
                        type: string
                      targetPort:
                        description: Target port
                        type: string
                      targetType:
                        description: Target type
                        type: string
                      type:
                        default: pty
                        description: Console type (e.g., "pty")
                        type: string
                    type: object
                  disk:
                    description: Disk devices
                    items:
                      description: DomainDisk represents a disk device
                      properties:
                        bootOrder:
                          description: Boot priority for this disk (1 = highest priority)
                          format: int32
                          type: integer
                        device:
                          description: Device target name (vda, vdb, etc.) - auto-assigned
                            if not specified
                          type: string
                        file:
                          description: Block file for the disk (fallback when no volume
                            reference)
                          type: string
                        type:
                          default: virtio
                          description: SCSI or virtio, etc.
                          type: string
                        volumeId:
                          description: Volume source reference (deprecated - use volumeRef)
                          type: string
                        volumeRef:
                          description: Volume resource reference (preferred)
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        wwn:
                          description: WWN (World Wide Name) for the disk
                          type: string
                      type: object
                    type: array
                  graphics:
                    description: Graphics configuration
                    properties:
                      autoport:
                        default: true
                        description: Autoport
                        type: boolean
                      listenAddress:
                        default: 127.0.0.1
                        description: Listen address
                        type: string
                      listenType:
                        default: address
                        description: Listen type
                        type: string
                      type:
                        default: spice
                        description: Graphics type (e.g., "spice", "vnc")
                        type: string
                    type: object
                  machine:
                    description: Machine type
                    type: string
                  memory:
                    description: Memory size in bytes
                    format: int64
                    type: integer
                  name:
                    description: Name of the domain
                    type: string
                  networkInterface:
                    description: Network interfaces
                    items:
                      description: DomainNetworkInterface represents a network interface
                      properties:
                        bridge:
                          description: Bridge name for bridged networking
                          type: string
                        device:
                          description: Interface device name (eth0, ens3, etc.) -
                            auto-assigned if not specified
                          type: string
                        hostname:
                          description: Hostname for the interface
                          type: string
                        mac:
                          description: MAC address (auto-generated if not specified)
                          type: string
                        model:
                          default: virtio
                          description: Model type (e.g., "virtio")
                          type: string
                        networkName:
                          description: Network name or bridge (fallback when no network
                            reference)
                          type: string
                        networkRef:
                          description: Network resource reference (preferred)
                          properties:
                            name:
                              description: Name of the referenced object.
                              type: string
                            policy:
                              description: Policies for referencing.
                              properties:
                                resolution:
                                  default: Required
                                  description: |-
                                    Resolution specifies whether resolution of this reference is required.
                                    The default is 'Required', which means the reconcile will fail if the
                                    reference cannot be resolved. 'Optional' means this reference will be
                                    a no-op if it cannot be resolved.
                                  enum:
                                  - Required
                                  - Optional
                                  type: string
                                resolve:
                                  description: |-
                                    Resolve specifies when this reference should be resolved. The default
                                    is 'IfNotPresent', which will attempt to resolve the reference only when
                                    the corresponding field is not present. Use 'Always' to resolve the
                                    reference on every reconcile.
                                  enum:
                                  - Always
                                  - IfNotPresent
                                  type: string
                              type: object
                          required:
                          - name
                          type: object
                        waitForLease:
                          default: true
                          description: Wait for lease
                          type: boolean
                      type: object
                    type: array
                  running:
                    default: true
                    description: Running state of the domain
                    type: boolean
                  type:
                    default: kvm
                    description: Domain type (e.g., "kvm", "qemu")
                    type: string
                  vcpu:
                    description: Number of virtual CPUs
                    format: int32
                    type: integer
                required:
                - memory
                - name
                - vcpu
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: DomainStatus defines the observed state of Domain
            properties:
              atProvider:
                description: DomainObservation are the observable fields of a Domain.
                properties:
                  id:
                    description: ID is the libvirt domain ID
                    type: string
                  networkInterfaces:
                    description: IP addresses assigned to network interfaces
                    items:
                      description: DomainNetworkInterfaceStatus represents the status
                        of a network interface
                      properties:
                        addresses:
                          description: IP addresses
                          items:
                            type: string
                          type: array
                        hostname:
                          description: Hostname
                          type: string
                        mac:
                          description: MAC address
                          type: string
                        name:
                          description: Name of the interface
                          type: string
                      type: object
                    type: array
                  state:
                    description: State of the domain (running, shutoff, etc.)
                    type: string
                  uuid:
                    description: UUID is the libvirt domain UUID
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
