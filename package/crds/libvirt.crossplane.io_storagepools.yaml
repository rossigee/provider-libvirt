---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  name: storagepools.libvirt.crossplane.io
spec:
  group: libvirt.crossplane.io
  names:
    kind: StoragePool
    listKind: StoragePoolList
    plural: storagepools
    singular: storagepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .spec.forProvider.type
      name: TYPE
      type: string
    - jsonPath: .status.atProvider.state
      name: STATE
      type: string
    - jsonPath: .status.atProvider.capacity
      name: CAPACITY
      type: string
    - jsonPath: .status.atProvider.available
      name: AVAILABLE
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A StoragePool is a libvirt storage pool.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StoragePoolSpec defines the desired state of StoragePool
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: StoragePoolParameters are the configurable fields of
                  a StoragePool.
                properties:
                  autoStart:
                    default: true
                    description: AutoStart determines if pool starts automatically
                    type: boolean
                  capacity:
                    description: Capacity of the storage pool in bytes (for supported
                      pool types)
                    format: int64
                    minimum: 0
                    type: integer
                  name:
                    description: Name of the storage pool
                    type: string
                  permissions:
                    description: Permissions for the storage pool
                    properties:
                      group:
                        description: Group of the storage pool
                        format: int32
                        type: integer
                      label:
                        description: SELinux label
                        type: string
                      mode:
                        description: Mode (permissions) of the storage pool
                        format: int32
                        type: integer
                      owner:
                        description: Owner of the storage pool
                        format: int32
                        type: integer
                    type: object
                  source:
                    description: Source configuration for the storage pool
                    properties:
                      adapter:
                        description: Adapter for SCSI storage
                        properties:
                          name:
                            description: Name of the adapter
                            type: string
                          parentFabricWwn:
                            description: Parent fabric WWN for FC adapters
                            type: string
                          parentWwn:
                            description: Parent WWN for FC adapters
                            type: string
                          type:
                            description: Type of adapter (scsi_host, fc_host)
                            enum:
                            - scsi_host
                            - fc_host
                            type: string
                          wwn:
                            description: WWN for FC adapters
                            type: string
                          wwnn:
                            description: WWNN for FC adapters
                            type: string
                          wwpn:
                            description: WWPN for FC adapters
                            type: string
                        required:
                        - type
                        type: object
                      auth:
                        description: Authentication for network storage
                        properties:
                          secret:
                            description: Secret reference for authentication
                            properties:
                              type:
                                description: Type of secret (iscsi, ceph, usage)
                                type: string
                              usage:
                                description: Usage ID for the secret
                                type: string
                              uuid:
                                description: UUID of the secret
                                type: string
                            required:
                            - type
                            type: object
                          type:
                            description: Type of authentication (chap, ceph)
                            enum:
                            - chap
                            - ceph
                            type: string
                          username:
                            description: Username for authentication
                            type: string
                        required:
                        - secret
                        - type
                        - username
                        type: object
                      device:
                        description: Device for block-based storage (disk, logical)
                        properties:
                          freeExtents:
                            description: Free extents information (for logical pools)
                            properties:
                              end:
                                description: End sector
                                format: int64
                                minimum: 0
                                type: integer
                              start:
                                description: Start sector
                                format: int64
                                minimum: 0
                                type: integer
                            required:
                            - end
                            - start
                            type: object
                          partTable:
                            description: Part table type (for disk pools)
                            enum:
                            - dos
                            - gpt
                            - mac
                            - bsd
                            - pc98
                            - sun
                            - lvm2
                            type: string
                          path:
                            description: Path to the device
                            type: string
                        required:
                        - path
                        type: object
                      dir:
                        description: Directory for directory-based storage
                        type: string
                      format:
                        description: Format for the source
                        properties:
                          type:
                            description: Type of format
                            type: string
                          vendor:
                            description: Vendor for the format
                            type: string
                        required:
                        - type
                        type: object
                      host:
                        description: Host for network-based storage (netfs, iscsi,
                          rbd, gluster)
                        properties:
                          name:
                            description: Name or IP address of the host
                            type: string
                          port:
                            description: Port number
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        required:
                        - name
                        type: object
                      name:
                        description: Name of the source (for various pool types)
                        type: string
                      product:
                        description: Product information
                        type: string
                      vendor:
                        description: Vendor information
                        type: string
                    type: object
                  target:
                    description: Target configuration for the storage pool
                    properties:
                      encryption:
                        description: Encryption settings for the target
                        properties:
                          format:
                            description: Encryption format (luks)
                            enum:
                            - luks
                            type: string
                          secret:
                            description: Secret containing encryption key
                            properties:
                              type:
                                description: Type of secret (passphrase)
                                type: string
                              usage:
                                description: Usage type for the secret
                                type: string
                              uuid:
                                description: UUID of the secret
                                type: string
                            required:
                            - type
                            type: object
                        required:
                        - format
                        - secret
                        type: object
                      path:
                        description: Path for the storage pool target
                        type: string
                      permissions:
                        description: Permissions for the target
                        properties:
                          group:
                            description: Group of the storage pool
                            format: int32
                            type: integer
                          label:
                            description: SELinux label
                            type: string
                          mode:
                            description: Mode (permissions) of the storage pool
                            format: int32
                            type: integer
                          owner:
                            description: Owner of the storage pool
                            format: int32
                            type: integer
                        type: object
                    required:
                    - path
                    type: object
                  type:
                    description: Type of the storage pool (dir, fs, netfs, iscsi,
                      scsi, mpath, disk, logical, zfs, rbd, gluster)
                    enum:
                    - dir
                    - fs
                    - netfs
                    - iscsi
                    - scsi
                    - mpath
                    - disk
                    - logical
                    - zfs
                    - rbd
                    - gluster
                    - sheepdog
                    - vstorage
                    type: string
                required:
                - name
                - target
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: StoragePoolStatus defines the observed state of StoragePool
            properties:
              atProvider:
                description: StoragePoolObservation are the observable fields of a
                  StoragePool.
                properties:
                  active:
                    description: Active indicates if the storage pool is currently
                      active
                    type: boolean
                  allocation:
                    description: Allocation of the storage pool in bytes
                    format: int64
                    type: integer
                  autoStart:
                    description: AutoStart indicates if the storage pool will auto-start
                    type: boolean
                  available:
                    description: Available space in the storage pool in bytes
                    format: int64
                    type: integer
                  capacity:
                    description: Capacity of the storage pool in bytes
                    format: int64
                    type: integer
                  persistent:
                    description: Persistent indicates if the storage pool is persistent
                    type: boolean
                  state:
                    description: State of the storage pool (inactive, active)
                    type: string
                  uuid:
                    description: UUID is the libvirt storage pool UUID
                    type: string
                  volumeCount:
                    description: VolumeCount is the number of volumes in the pool
                    format: int32
                    type: integer
                  volumes:
                    description: Volumes in the storage pool
                    items:
                      description: StoragePoolVolume represents a volume in the storage
                        pool
                      properties:
                        allocation:
                          description: Allocation of the volume in bytes
                          format: int64
                          type: integer
                        capacity:
                          description: Capacity of the volume in bytes
                          format: int64
                          type: integer
                        key:
                          description: Key of the volume
                          type: string
                        name:
                          description: Name of the volume
                          type: string
                        path:
                          description: Path of the volume
                          type: string
                        type:
                          description: Type of the volume
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
