---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: storagepools.libvirt.m.crossplane.io
spec:
  group: libvirt.m.crossplane.io
  names:
    categories:
    - crossplane
    - managed
    - libvirt
    kind: StoragePool
    listKind: StoragePoolList
    plural: storagepools
    singular: storagepool
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .metadata.annotations.crossplane\.io/external-name
      name: EXTERNAL-NAME
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1beta1
    schema:
      openAPIV3Schema:
        description: A StoragePool is a managed resource that represents a libvirt
          storage pool.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: StoragePoolSpec defines the desired state of StoragePool
            properties:
              forProvider:
                description: StoragePoolParameters are the configurable fields of
                  a StoragePool.
                properties:
                  autostart:
                    default: true
                    description: Autostart determines if pool starts automatically
                    type: boolean
                  capacity:
                    description: Capacity of the storage pool in bytes (for supported
                      pool types)
                    format: int64
                    minimum: 0
                    type: integer
                  name:
                    description: Name of the storage pool
                    type: string
                  permissions:
                    description: Permissions for the storage pool
                    properties:
                      group:
                        description: Group of the pool
                        type: string
                      label:
                        description: Label for SELinux
                        type: string
                      mode:
                        description: Mode of the pool (e.g., 0755)
                        type: string
                      owner:
                        description: Owner of the pool
                        type: string
                    type: object
                  source:
                    description: Source configuration for the storage pool
                    properties:
                      device:
                        description: Device for block-based storage (disk, logical)
                        properties:
                          path:
                            description: Path to the device
                            type: string
                        required:
                        - path
                        type: object
                      dir:
                        description: Directory for directory-based storage
                        type: string
                      format:
                        description: Format for the source
                        properties:
                          type:
                            description: Type of the format
                            type: string
                        required:
                        - type
                        type: object
                      host:
                        description: Host for network-based storage (netfs, iscsi,
                          rbd, gluster)
                        properties:
                          name:
                            description: Name or IP address of the host
                            type: string
                          port:
                            description: Port number for the connection
                            format: int32
                            type: integer
                        required:
                        - name
                        type: object
                      name:
                        description: Name of the source (for various pool types)
                        type: string
                    type: object
                  target:
                    description: Target configuration for the storage pool
                    properties:
                      path:
                        description: Path for the storage pool target
                        type: string
                      permissions:
                        description: Permissions for the target
                        properties:
                          group:
                            description: Group of the pool
                            type: string
                          label:
                            description: Label for SELinux
                            type: string
                          mode:
                            description: Mode of the pool (e.g., 0755)
                            type: string
                          owner:
                            description: Owner of the pool
                            type: string
                        type: object
                    required:
                    - path
                    type: object
                  type:
                    description: Type of the storage pool (dir, fs, netfs, iscsi,
                      scsi, mpath, disk, logical, zfs, rbd, gluster)
                    enum:
                    - dir
                    - fs
                    - netfs
                    - iscsi
                    - scsi
                    - mpath
                    - disk
                    - logical
                    - zfs
                    - rbd
                    - gluster
                    - sheepdog
                    - vstorage
                    type: string
                required:
                - name
                - target
                - type
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  kind: ClusterProviderConfig
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  kind:
                    description: Kind of the referenced object.
                    type: string
                  name:
                    description: Name of the referenced object.
                    type: string
                required:
                - kind
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                required:
                - name
                type: object
            required:
            - forProvider
            type: object
          status:
            description: StoragePoolStatus defines the observed state of StoragePool
            properties:
              atProvider:
                description: StoragePoolObservation are the observable fields of a
                  StoragePool.
                properties:
                  active:
                    description: Active status of the storage pool
                    type: boolean
                  allocation:
                    description: Allocation of the storage pool in bytes
                    format: int64
                    type: integer
                  autoStart:
                    description: AutoStart status of the storage pool
                    type: boolean
                  available:
                    description: Available space in the storage pool in bytes
                    format: int64
                    type: integer
                  capacity:
                    description: Capacity of the storage pool in bytes
                    format: int64
                    type: integer
                  name:
                    description: Name of the storage pool
                    type: string
                  persistent:
                    description: Persistent status of the storage pool
                    type: boolean
                  state:
                    description: State of the storage pool
                    type: string
                  uuid:
                    description: UUID of the storage pool in libvirt
                    type: string
                  volumeCount:
                    description: Number of volumes in the storage pool
                    type: integer
                  volumes:
                    description: Volumes in the storage pool
                    items:
                      description: StoragePoolVolume represents a volume in a storage
                        pool
                      properties:
                        allocation:
                          description: Allocation of the volume
                          format: int64
                          type: integer
                        capacity:
                          description: Capacity of the volume
                          format: int64
                          type: integer
                        key:
                          description: Key of the volume
                          type: string
                        name:
                          description: Name of the volume
                          type: string
                        path:
                          description: Path to the volume
                          type: string
                        type:
                          description: Type of the volume
                          type: string
                      type: object
                    type: array
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastHandledReconcileAt:
                description: |-
                  LastHandledReconcileAt holds the value of the most recent
                  reconcile-requested-at annotation token that the controller has
                  processed. Users can compare this to the annotation to determine
                  whether a reconcile request has been handled.
                type: string
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
