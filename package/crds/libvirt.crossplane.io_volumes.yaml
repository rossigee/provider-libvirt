---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.0
  name: volumes.libvirt.crossplane.io
spec:
  group: libvirt.crossplane.io
  names:
    kind: Volume
    listKind: VolumeList
    plural: volumes
    singular: volume
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.conditions[?(@.type=='Ready')].status
      name: READY
      type: string
    - jsonPath: .status.conditions[?(@.type=='Synced')].status
      name: SYNCED
      type: string
    - jsonPath: .status.atProvider.pool
      name: POOL
      type: string
    - jsonPath: .status.atProvider.capacity
      name: CAPACITY
      type: string
    - jsonPath: .status.atProvider.format
      name: FORMAT
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: AGE
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: A Volume is a libvirt storage volume.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VolumeSpec defines the desired state of Volume
            properties:
              deletionPolicy:
                default: Delete
                description: |-
                  DeletionPolicy specifies what will happen to the underlying external
                  when this managed resource is deleted - either "Delete" or "Orphan" the
                  external resource.
                  This field is planned to be deprecated in favor of the ManagementPolicies
                  field in a future release. Currently, both could be set independently and
                  non-default values would be honored if the feature flag is enabled.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                enum:
                - Orphan
                - Delete
                type: string
              forProvider:
                description: VolumeParameters are the configurable fields of a Volume.
                properties:
                  allocation:
                    description: Allocation of the volume in bytes (for sparse files)
                    format: int64
                    type: integer
                  backingStore:
                    description: Base volume for backing store (for qcow2)
                    properties:
                      format:
                        default: qcow2
                        description: Format of the backing store
                        type: string
                      path:
                        description: Path to the backing store file
                        type: string
                    required:
                    - path
                    type: object
                  capacity:
                    description: Capacity of the volume in bytes
                    format: int64
                    minimum: 1048576
                    type: integer
                  encryption:
                    description: Encryption settings for the volume
                    properties:
                      format:
                        description: Encryption format (luks, qcow)
                        enum:
                        - luks
                        - qcow
                        type: string
                      secret:
                        description: Secret containing encryption key (legacy - use
                          SecretRef instead)
                        properties:
                          type:
                            description: Type of secret (passphrase, aes)
                            type: string
                          usage:
                            description: Usage type for the secret
                            type: string
                          uuid:
                            description: UUID of the secret
                            type: string
                        required:
                        - type
                        type: object
                      secretRef:
                        description: SecretRef references a Secret resource for encryption
                        properties:
                          name:
                            description: Name of the referenced object.
                            type: string
                          policy:
                            description: Policies for referencing.
                            properties:
                              resolution:
                                default: Required
                                description: |-
                                  Resolution specifies whether resolution of this reference is required.
                                  The default is 'Required', which means the reconcile will fail if the
                                  reference cannot be resolved. 'Optional' means this reference will be
                                  a no-op if it cannot be resolved.
                                enum:
                                - Required
                                - Optional
                                type: string
                              resolve:
                                description: |-
                                  Resolve specifies when this reference should be resolved. The default
                                  is 'IfNotPresent', which will attempt to resolve the reference only when
                                  the corresponding field is not present. Use 'Always' to resolve the
                                  reference on every reconcile.
                                enum:
                                - Always
                                - IfNotPresent
                                type: string
                            type: object
                        required:
                        - name
                        type: object
                    required:
                    - format
                    type: object
                  format:
                    default: qcow2
                    description: Format of the volume (qcow2, raw, vmdk, etc.)
                    type: string
                  name:
                    description: Name of the volume
                    type: string
                  pool:
                    description: Storage pool name where the volume will be created
                    type: string
                  source:
                    description: Source volume to clone from
                    properties:
                      file:
                        description: File path to copy from (for file-based pools)
                        type: string
                      pool:
                        description: Pool name where the source volume resides
                        type: string
                      url:
                        description: URL to download volume from
                        type: string
                      volume:
                        description: Volume name to clone from
                        type: string
                    type: object
                  target:
                    description: Target configuration for the volume
                    properties:
                      compat:
                        description: Compatibility settings
                        type: string
                      features:
                        description: Features enabled for the volume
                        items:
                          type: string
                        type: array
                      format:
                        description: Format for the target
                        type: string
                      path:
                        description: Path where the volume will be accessible
                        type: string
                      permissions:
                        description: Permissions for the volume
                        properties:
                          group:
                            description: Group of the volume
                            format: int32
                            type: integer
                          label:
                            description: SELinux label
                            type: string
                          mode:
                            description: Mode (permissions) of the volume
                            format: int32
                            type: integer
                          owner:
                            description: Owner of the volume
                            format: int32
                            type: integer
                        type: object
                      timestamps:
                        description: Timestamps configuration
                        properties:
                          atime:
                            description: Access time
                            format: date-time
                            type: string
                          ctime:
                            description: Creation time
                            format: date-time
                            type: string
                          mtime:
                            description: Modification time
                            format: date-time
                            type: string
                        type: object
                    type: object
                required:
                - capacity
                - name
                - pool
                type: object
              managementPolicies:
                default:
                - '*'
                description: |-
                  THIS IS A BETA FIELD. It is on by default but can be opted out
                  through a Crossplane feature flag.
                  ManagementPolicies specify the array of actions Crossplane is allowed to
                  take on the managed and external resources.
                  This field is planned to replace the DeletionPolicy field in a future
                  release. Currently, both could be set independently and non-default
                  values would be honored if the feature flag is enabled. If both are
                  custom, the DeletionPolicy field will be ignored.
                  See the design doc for more information: https://github.com/crossplane/crossplane/blob/499895a25d1a1a0ba1604944ef98ac7a1a71f197/design/design-doc-observe-only-resources.md?plain=1#L223
                  and this one: https://github.com/crossplane/crossplane/blob/444267e84783136daa93568b364a5f01228cacbe/design/one-pager-ignore-changes.md
                items:
                  description: |-
                    A ManagementAction represents an action that the Crossplane controllers
                    can take on an external resource.
                  enum:
                  - Observe
                  - Create
                  - Update
                  - Delete
                  - LateInitialize
                  - '*'
                  type: string
                type: array
              providerConfigRef:
                default:
                  name: default
                description: |-
                  ProviderConfigReference specifies how the provider that will be used to
                  create, observe, update, and delete this managed resource should be
                  configured.
                properties:
                  name:
                    description: Name of the referenced object.
                    type: string
                  policy:
                    description: Policies for referencing.
                    properties:
                      resolution:
                        default: Required
                        description: |-
                          Resolution specifies whether resolution of this reference is required.
                          The default is 'Required', which means the reconcile will fail if the
                          reference cannot be resolved. 'Optional' means this reference will be
                          a no-op if it cannot be resolved.
                        enum:
                        - Required
                        - Optional
                        type: string
                      resolve:
                        description: |-
                          Resolve specifies when this reference should be resolved. The default
                          is 'IfNotPresent', which will attempt to resolve the reference only when
                          the corresponding field is not present. Use 'Always' to resolve the
                          reference on every reconcile.
                        enum:
                        - Always
                        - IfNotPresent
                        type: string
                    type: object
                required:
                - name
                type: object
              publishConnectionDetailsTo:
                description: |-
                  PublishConnectionDetailsTo specifies the connection secret config which
                  contains a name, metadata and a reference to secret store config to
                  which any connection details for this managed resource should be written.
                  Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                properties:
                  configRef:
                    default:
                      name: default
                    description: |-
                      SecretStoreConfigRef specifies which secret store config should be used
                      for this ConnectionSecret.
                    properties:
                      name:
                        description: Name of the referenced object.
                        type: string
                      policy:
                        description: Policies for referencing.
                        properties:
                          resolution:
                            default: Required
                            description: |-
                              Resolution specifies whether resolution of this reference is required.
                              The default is 'Required', which means the reconcile will fail if the
                              reference cannot be resolved. 'Optional' means this reference will be
                              a no-op if it cannot be resolved.
                            enum:
                            - Required
                            - Optional
                            type: string
                          resolve:
                            description: |-
                              Resolve specifies when this reference should be resolved. The default
                              is 'IfNotPresent', which will attempt to resolve the reference only when
                              the corresponding field is not present. Use 'Always' to resolve the
                              reference on every reconcile.
                            enum:
                            - Always
                            - IfNotPresent
                            type: string
                        type: object
                    required:
                    - name
                    type: object
                  metadata:
                    description: Metadata is the metadata for connection secret.
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          Annotations are the annotations to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.annotations".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          Labels are the labels/tags to be added to connection secret.
                          - For Kubernetes secrets, this will be used as "metadata.labels".
                          - It is up to Secret Store implementation for others store types.
                        type: object
                      type:
                        description: |-
                          Type is the SecretType for the connection secret.
                          - Only valid for Kubernetes Secret Stores.
                        type: string
                    type: object
                  name:
                    description: Name is the name of the connection secret.
                    type: string
                required:
                - name
                type: object
              writeConnectionSecretToRef:
                description: |-
                  WriteConnectionSecretToReference specifies the namespace and name of a
                  Secret to which any connection details for this managed resource should
                  be written. Connection details frequently include the endpoint, username,
                  and password required to connect to the managed resource.
                  This field is planned to be replaced in a future release in favor of
                  PublishConnectionDetailsTo. Currently, both could be set independently
                  and connection details would be published to both without affecting
                  each other.
                properties:
                  name:
                    description: Name of the secret.
                    type: string
                  namespace:
                    description: Namespace of the secret.
                    type: string
                required:
                - name
                - namespace
                type: object
            required:
            - forProvider
            type: object
          status:
            description: VolumeStatus defines the observed state of Volume
            properties:
              atProvider:
                description: VolumeObservation are the observable fields of a Volume.
                properties:
                  allocation:
                    description: Allocation of the volume in bytes
                    format: int64
                    type: integer
                  backingStore:
                    description: BackingStore information if present
                    properties:
                      allocation:
                        description: Allocation of the backing store
                        format: int64
                        type: integer
                      capacity:
                        description: Capacity of the backing store
                        format: int64
                        type: integer
                      format:
                        description: Format of the backing store
                        type: string
                      path:
                        description: Path to the backing store
                        type: string
                    type: object
                  capacity:
                    description: Capacity of the volume in bytes
                    format: int64
                    type: integer
                  creationTime:
                    description: CreationTime when the volume was created
                    format: date-time
                    type: string
                  format:
                    description: Format of the volume
                    type: string
                  key:
                    description: Key is the libvirt volume key
                    type: string
                  modificationTime:
                    description: ModificationTime when the volume was last modified
                    format: date-time
                    type: string
                  path:
                    description: Path is the full path to the volume
                    type: string
                  physical:
                    description: Physical size of the volume in bytes
                    format: int64
                    type: integer
                  pool:
                    description: Pool the volume belongs to
                    type: string
                  type:
                    description: Type of the volume (file, block, dir, network)
                    type: string
                type: object
              conditions:
                description: Conditions of the resource.
                items:
                  description: A Condition that may apply to a resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        LastTransitionTime is the last time this condition transitioned from one
                        status to another.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        A Message containing details about this condition's last transition from
                        one status to another, if any.
                      type: string
                    observedGeneration:
                      description: |-
                        ObservedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      type: integer
                    reason:
                      description: A Reason for this condition's last transition from
                        one status to another.
                      type: string
                    status:
                      description: Status of this condition; is it currently True,
                        False, or Unknown?
                      type: string
                    type:
                      description: |-
                        Type of this condition. At most one of each condition type may apply to
                        a resource at any point in time.
                      type: string
                  required:
                  - lastTransitionTime
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              observedGeneration:
                description: |-
                  ObservedGeneration is the latest metadata.generation
                  which resulted in either a ready state, or stalled due to error
                  it can not recover from without human intervention.
                format: int64
                type: integer
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
